{
  "name": "privateness-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for Privateness CLI endpoints with HTTP JSON interface",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["privateness", "skycoin", "mcp", "json-rpc", "blockchain", "cryptocurrency"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
